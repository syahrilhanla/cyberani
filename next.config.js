/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ["gogocdn.net", "goload.pro", "cdn.noitatnemucod.net"],
	},
};

module.exports = nextConfig;
